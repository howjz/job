how:
  job:
    enable: true
    type: redis
    debug:
      sleep: 0
      log: false
    pool:
      size: 10

server:
  port: 15000

spring:
  redis:
    database: 0
    host: 192.168.99.100
    port: 63791
    lettuce:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 8
        min-idle: 0
        timeout: 5000

